---
// src/components/ServicePreviewCard.astro
// Este componente muestra una vista previa compacta de un servicio.
// Es utilizado en la página principal de servicios para destacar los "más solicitados".

interface Service {
  title: string;
  slug: string;
  description: string;
  whatsappLink: string;
  imageUrls: string[];
  previewImageUrl: string;
  category: string;
  isMostClicked?: boolean;
}

interface Props {
  service: Service;
  className?: string;
}

const { service, className = "" } = Astro.props; // Desestructura el objeto de servicio.
---

<div class={`bg-gradient-to-br from-white via-blue-50 to-slate-100 rounded-3xl shadow-2xl p-8 flex flex-col items-center text-center transition-all duration-300 hover:shadow-3xl hover:scale-105 border border-[#c8aa6e]/20 min-h-[420px] max-h-[540px] h-auto w-full ${className}`}>
  <img src={service.previewImageUrl} alt={service.title} class="w-full h-48 sm:h-56 object-cover rounded-2xl mb-6 shadow-lg border-2 border-blue-100">
  <h3 class="text-3xl sm:text-4xl font-extrabold text-[#1e3a5f] mb-8 mt-2 tracking-tight drop-shadow-lg leading-tight flex-1 flex items-center justify-center">{service.title}</h3>
  <a href={`/servicios/${service.slug}`} class="inline-block bg-gradient-to-r from-[#1e3a5f] to-[#4a90e2] text-white px-8 py-4 rounded-xl text-lg font-bold hover:from-[#4a90e2] hover:to-[#1e3a5f] transition-all duration-300 mt-auto shadow-lg border-2 border-[#c8aa6e]/30 w-full focus:outline-none focus:ring-2 focus:ring-[#c8aa6e] focus:ring-offset-2 active:scale-95">Más Información</a>
</div>
