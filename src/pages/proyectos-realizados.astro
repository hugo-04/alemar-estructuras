---
import BaseLayout from '../layouts/BaseLayout.astro';
import TestimonialCard from '../components/TestimonialCard.astro';
import { testimonials } from '../data/testimonials.ts';
---

<BaseLayout title="Proyectos Realizados - [Nombre de tu Empresa]">
  <main class="container mx-auto p-6 mt-8 flex-grow">
    <section class="text-center mb-16">
      <h2 class="text-4xl font-extrabold text-gray-800 mb-4">Nuestros Proyectos y Testimonios</h2>
      <p class="text-lg text-gray-600 max-w-3xl mx-auto">La satisfacción de nuestros clientes constituye nuestra principal carta de presentación. A continuación, exponemos una selección de nuestros trabajos más representativos y los testimonios de quienes han depositado su confianza en nosotros.</p>
    </section>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
      {
        testimonials.map((testimonial) => (
          <TestimonialCard
            imageUrl={testimonial.imageUrl}
            altText={testimonial.altText}
            quote={testimonial.quote}
            clientName={testimonial.clientName}
          />
        ))
      }
    </div>
  </main>
</BaseLayout>
