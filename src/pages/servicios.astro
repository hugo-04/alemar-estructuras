---
// src/pages/servicios.astro
// Esta página muestra una visión general de los servicios ofrecidos.
// Incluye una sección de servicios destacados y una llamada a la acción.

import Layout from '../layouts/Layout.astro'; // Importa el layout principal de la página.
import ServicePreviewCard from '../components/ServicePreviewCard.astro'; // Importa el componente para la vista previa de servicios.
import { services } from '../data/services.ts'; // Importa los datos de todos los servicios.

// Filtra los servicios que tienen la propiedad 'isMostClicked' establecida en true.
// Estos servicios se mostrarán en la sección "Servicios Más Solicitados".
const mostClickedServices = services.filter(service => service.isMostClicked);
---

<Layout title="Nuestros Servicios - [Nombre de tu Empresa]">
  <!-- Sección Hero: Banner principal de la página de servicios. -->
  <section class="relative bg-cover bg-center h-96 flex items-center justify-center text-white shadow-lg"
    style="background-image: url('https://via.placeholder.com/1920x600/1A202C/FFFFFF?text=Nuestros+Servicios');">
    <div class="absolute inset-0 bg-gray-900 opacity-80"></div>
    <div class="relative z-10 text-center p-4">
      <h1 class="text-5xl font-extrabold mb-4">Nuestros Servicios Integrales</h1>
      <p class="text-xl max-w-2xl mx-auto">Explore nuestra gama de servicios especializados y encuentre la solución óptima para sus requerimientos.</p>
    </div>
  </section>

  <main class="container mx-auto p-6 mt-8 flex-grow">
    <!-- Sección de Servicios Más Solicitados: Muestra los servicios marcados como destacados. -->
    <section class="text-center mb-16">
      <h2 class="text-4xl font-extrabold text-gray-800 mb-6">Servicios Destacados</h2>
      <p class="text-lg text-gray-600 max-w-4xl mx-auto leading-relaxed">
        A continuación, presentamos los servicios más valorados y solicitados por nuestros clientes. Le invitamos a hacer clic en cada uno para obtener información detallada.
      </p>
    </section>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
      {
        mostClickedServices.map(service => (
          <ServicePreviewCard service={service} />
        ))
      }
    </div>

    <!-- Sección de Llamada a la Acción (CTA): Invita a los usuarios a contactar. -->
    <section class="bg-gray-800 text-white text-center p-12 rounded-lg shadow-lg mt-20">
      <h2 class="text-4xl font-extrabold mb-4">¿Preparado para iniciar su proyecto?</h2>
      <p class="text-xl mb-8">Contáctenos hoy mismo para recibir una asesoría sin compromiso y descubrir cómo podemos contribuir al éxito de su proyecto.</p>
      <a href="/contacto" class="inline-block bg-gray-900 text-white px-10 py-5 rounded-lg text-xl font-semibold hover:bg-black transition-colors duration-300 shadow-md">
        Contactar Ahora
      </a>
    </section>
  </main>
</Layout>
